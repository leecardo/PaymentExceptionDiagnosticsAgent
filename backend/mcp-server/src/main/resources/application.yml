server:
  port: 8081

spring:
  application:
    name: payment-diagnostics-mcp-server

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
